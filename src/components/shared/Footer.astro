---
// ✅ Iconos actualizados: LinkedIn en vez de Twitter
import { Facebook, Linkedin, Instagram, Mail, Phone, MapPin } from 'lucide-react';

const currentYear = new Date().getFullYear();

const navigation = {
  explore: [
    { href: '/quienes-somos', label: 'Quiénes Somos' },
    { href: '/programas', label: 'Nuestros Proyectos' },
    { href: '/transparencia', label: 'Nuestro Equipo' },
    { href: '/noticias', label: 'Blog y Noticias' }
  ],
  legal: [
    { href: '/privacidad', label: 'Política de Privacidad' },
    { href: '/terminos', label: 'Términos de Uso' }
  ]
};

const contactInfo = [
  {
    icon: MapPin,
    text: 'Calle Graciela Pérez N° 055,',
    subtext: 'Barrio Nazaria, Cobija, Pando.',
    type: 'address'
  },
  {
    icon: Phone,
    text: '(Pando-Bolivia) +591 71114083',
    href: 'tel:+59171114083',
    type: 'phone'
  },
  {
    icon: Mail,
    text: 'contacto@forama.org',
    href: 'mailto:contacto@forama.org',
    type: 'email'
  },
  {
    icon: Mail,
    text: 'proyectos@forama.org',
    href: 'mailto:proyectos@forama.org',
    type: 'email'
  }
];

// ✅ ACTUALIZADO: LinkedIn y X en vez de Twitter
const socialMedia = [
  { 
    href: 'https://www.facebook.com/profile.php?id=61587677847777', 
    icon: Facebook, 
    label: 'Facebook',
    name: 'Facebook de FORAMA',
    color: 'hover:bg-blue-600'
  },
  { 
    href: 'https://www.linkedin.com/in/fundación-flora-amazonica-forama-7026533ab', 
    icon: Linkedin, 
    label: 'LinkedIn',
    name: 'LinkedIn de FORAMA',
    color: 'hover:bg-blue-700'
  },
  { 
    href: 'https://x.com/Fundacionforama', 
    icon: 'X', // Usaremos SVG custom
    label: 'X',
    name: 'X (Twitter) de FORAMA',
    color: 'hover:bg-black'
  },
  { 
    href: 'https://www.instagram.com/fundacionforama/', 
    icon: Instagram, 
    label: 'Instagram',
    name: 'Instagram de FORAMA',
    color: 'hover:bg-gradient-to-br hover:from-purple-600 hover:to-pink-600'
  }
];
---

<footer class="bg-slate-950 text-slate-200 py-16 border-t border-slate-900 font-sans" aria-labelledby="footer-heading">
  <h2 id="footer-heading" class="sr-only">Pie de página</h2>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12 mb-12">
      
      <!-- Sección: Marca y Redes Sociales -->
      <div>
        <a 
          href="/" 
          class="text-2xl font-extrabold tracking-tighter text-white mb-6 block hover:text-earth-500 transition-colors"
          aria-label="Volver al inicio"
        >
          FORAMA<span class="text-earth-500"></span>
        </a>
        <p class="text-amazon-100/80 leading-relaxed mb-6 text-sm md:text-base">
          Impulsando el desarrollo sostenible en la Amazonía boliviana mediante la integración de ciencia, educación y saberes ancestrales.
        </p>
        
        {/* ✅ REDES SOCIALES ACTUALIZADAS */}
        <nav aria-label="Redes sociales">
          <ul class="flex gap-4">
            {socialMedia.map((social) => (
              <li>
                <a 
                  href={social.href}
                  class={`bg-amazon-800 ${social.color} p-2 rounded-full transition-all duration-300 inline-flex items-center justify-center hover:scale-110`}
                  aria-label={social.name}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {/* ✅ Icono de X personalizado */}
                  {social.icon === 'X' ? (
                    <svg 
                      width="20" 
                      height="20" 
                      viewBox="0 0 24 24" 
                      fill="currentColor"
                      aria-hidden="true"
                    >
                      <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                    </svg>
                  ) : (
                    <social.icon size={20} aria-hidden="true" />
                  )}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </div>

      <!-- Sección: Navegación -->
      <nav aria-labelledby="footer-nav-heading">
        <h3 id="footer-nav-heading" class="text-white font-bold text-lg mb-6">Explorar</h3>
        <ul class="space-y-3">
          {navigation.explore.map((link) => (
            <li>
              <a 
                href={link.href} 
                class="hover:text-white hover:translate-x-1 transition-all inline-block text-sm md:text-base"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>

      <!-- Sección: Contacto -->
      <div>
        <h3 class="text-white font-bold text-lg mb-6">Contacto</h3>
        <address class="not-italic space-y-4 text-sm md:text-base">
          {contactInfo.map((contact) => {
            const Icon = contact.icon;
            return (
              <div class="flex items-start gap-3">
                <Icon className="text-earth-500 shrink-0 mt-1" size={20} aria-hidden="true" />
                {contact.href ? (
                  <a 
                    href={contact.href} 
                    class="hover:text-white transition-colors"
                    {...(contact.type === 'email' && { 'aria-label': `Enviar correo a ${contact.text}` })}
                    {...(contact.type === 'phone' && { 'aria-label': `Llamar al ${contact.text}` })}
                  >
                    {contact.text}
                  </a>
                ) : (
                  <span>
                    {contact.text}
                    {contact.subtext && <><br />{contact.subtext}</>}
                  </span>
                )}
              </div>
            );
          })}
        </address>
      </div>

      <!-- Sección: CTA Donación -->
      <aside class="bg-amazon-800/50 p-6 rounded-2xl border border-amazon-700/50 h-fit" aria-labelledby="donation-heading">
        <h3 id="donation-heading" class="text-white font-bold text-lg mb-4">
          Tu apoyo hace la diferencia
        </h3>
        <p class="text-sm mb-6 text-amazon-200">
          Cada donación protege hectáreas reales de bosque en Pando.
        </p>
        <a 
          href="/como-apoyar" 
          class="block w-full bg-earth-500 hover:bg-earth-600 text-white font-bold py-3 px-4 rounded-xl text-center transition-colors shadow-lg hover:shadow-xl focus:ring-2 focus:ring-earth-400 focus:outline-none"
        >
          Hacer una Donación
        </a>
      </aside>

    </div>

    <!-- Footer Bottom -->
   <div class="border-t border-amazon-800 pt-8 flex flex-col gap-4 text-sm text-amazon-400">
  
  {/* Fila 1: Copyright + Legal */}
  <div class="flex flex-col md:flex-row justify-between items-center gap-4">
    <p class="text-center md:text-left">
      &copy; {currentYear} Fundación Flora Amazónica. Todos los derechos reservados.
    </p>
    
    <nav aria-label="Enlaces legales">
      <ul class="flex gap-6">
        {navigation.legal.map((link) => (
          <li>
            <a href={link.href} class="hover:text-white transition-colors">
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>

  {/* Fila 2: Atribución Pexels */}
  <p class="text-left text-xs text-slate-300 pb-2">
    Agradecemos a la comunidad de Pexels
    por los videos disponibles bajo licencia gratuita, que han contribuido a enriquecer el sitio institucional de FORAMA.
  </p>

</div>

  </div>
</footer>

<link rel="prefetch" href="/como-apoyar" />
<link rel="prefetch" href="/quienes-somos" />

<style>
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }
</style>